import styles from './Explore.module.scss';

export const Explore = () => {
  return (
    <div className={styles.container}>
      <button className={`textButton`}>Become a volunteer</button>
    </div>
  );
};
